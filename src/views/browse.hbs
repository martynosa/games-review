<main>
    <div class="container text-white">
        <h1 class="display-1 text-center mb-5 text-warning">Browse</h1>
        {{#if allGames}}
        <div class="row row-cols-1 row-cols-md-4 g-4">
            {{#each allGames}}
            <div class="col">
                <div class="card" style="background-color: #212529;">
                    <div class="card-header">{{title}}</div>
                    <img src={{imageUrl}} class="card-img-top" alt="...">
                    <div class="card-body">
                        <p class="card-text"><small class="text-muted">Ganre: {{ganre}}</small></p>
                        <p class="card-text"><small class="text-muted">Release year: {{releaseYear}}</small></p>
                        <p class="card-text">👍 {{rating}}</p>
                    </div>
                    <a href="/game/{{_id}}" class="btn btn-primary">Details</a>
                </div>
            </div>
            {{/each}}
            {{else}}
            <h3 class="display-1 text-center mb-5">Nothing to show...</h3>
            {{/if}}
        </div>
    </div>
</main>